# Root ignores. The Godot project has its own .gitignore in godot/.
# Local agent settings and secrets
.claude/
.env
*.log
# Legacy web version (reference only)
legacy/web/node_modules/
legacy/web/dist/
legacy/web/.env
